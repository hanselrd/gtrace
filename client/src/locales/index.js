import LocalizedStrings from 'react-localization';
import en from './en';
import es from './es';

export default new LocalizedStrings({ en, es });

// locales.home.user
// locales.formatString(locales.footer.link, locales.footer.three)
