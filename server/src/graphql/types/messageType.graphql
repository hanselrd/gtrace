type Message {
  id: ID!
  text: String!
  user: User!
  createdAt: DateTime!
}

type Query {
  messages: [Message!]
  message(id: Int!): Message
}

type Mutation {
  addMessage(text: String!): Message! @auth
}

type Subscription {
  messageAdded: Message! @auth
}

